# ==============================================================================
# 一键运行 CDC_US 仿真（自动编译 GOWIN 库）
# ==============================================================================

echo "CDC_US 仿真一键启动脚本"
echo "========================================"

# 检查是否需要编译 GOWIN 库
if {![file exists "./gw5a/_info"]} {
    echo "未找到 GOWIN 库，开始编译..."
    echo ""
    do compile_gowin_lib.do
    echo ""
    echo "GOWIN 库编译完成！"
    echo "========================================"
    echo ""
} else {
    echo "✓ GOWIN 库已存在，跳过编译"
    echo "========================================"
    echo ""
}

# 运行 CDC_US 仿真
echo "开始 CDC_US 仿真..."
do cmd_cdc_us.do
